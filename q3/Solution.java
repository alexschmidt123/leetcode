package q3;

public class Solution {
    public int lengthOfLongestSubstring(String s) {
        int l = 0, r = 0, res = 0;
        int len = s.length();
        if (len<=1){return len;}
        while(l<len && r<len){
            
        }
        return res;
    }
}
